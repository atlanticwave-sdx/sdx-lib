"""
Unit tests for the sdxlib library.

Run from the parent SDXLIB directory using:
    python -m unittest discover -v tests

It monitors your program, noting which parts
of the code have been executed, then analyzes
the source to identify code that could have
been executed but was not.

To utilize the coverage module:  
    coverage run -m unittest discover -v tests
    coverage report -m
"""
